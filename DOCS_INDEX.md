---
title: Docs Index — Map of Content
doc_type: index
status: living
updated: 2026-09-18
tags: [doc/index, status/living]
---

# Docs Index

Every project document, what it's for, and how much of it is **built** vs **planned**. This
folder is both a git repository and an [Obsidian](https://obsidian.md) vault — open the repo
root as a vault and this note is your front door.

> **Read [`PROJECT_STATE.md`](PROJECT_STATE.md) first.** It is the single source of truth for
> what is WORKING / MOCKED / NOT YET IMPLEMENTED. Every other doc is written against it, and
> where they disagree, `PROJECT_STATE.md` wins.

## Start here

| Doc | What it answers | Status |
|---|---|---|
| [`README.md`](README.md) | What is this, how do I run it | living |
| [`PROJECT_STATE.md`](PROJECT_STATE.md) | What is actually built right now | living |
| [`ROADMAP.md`](ROADMAP.md) | What comes next, Phases 0–10 | discovery |

## Design & architecture

| Doc | What it answers | Status |
|---|---|---|
| [`ARCHITECTURE.md`](ARCHITECTURE.md) | System shape, routing, auth, server actions | mixed |
| [`AI_ARCHITECTURE.md`](AI_ARCHITECTURE.md) | Provider abstraction, retrieval, citations | mixed |
| [`DATABASE.md`](DATABASE.md) | Prisma schema, models, relations | mixed |
| [`SECURITY.md`](SECURITY.md) | What is enforced today vs planned | mixed |

## Product & content

| Doc | What it answers | Status |
|---|---|---|
| [`PRODUCT_SPEC.md`](PRODUCT_SPEC.md) | Product philosophy and direction | discovery |
| [`CURRICULUM_SYSTEM.md`](CURRICULUM_SYSTEM.md) | Lesson versioning, prerequisites, concepts | built |
| [`HEYGEN_CONTENT_PIPELINE.md`](HEYGEN_CONTENT_PIPELINE.md) | Avatar video pipeline feasibility | proposal |

## Agent instructions

| Doc | What it answers | Status |
|---|---|---|
| [`CLAUDE.md`](CLAUDE.md) | Entry point Claude Code reads — includes `AGENTS.md` | living |
| [`AGENTS.md`](AGENTS.md) | Next.js rules. **Auto-generated by `next dev` — don't hand-edit** | generated |

## Reading the status tags

Every doc carries YAML frontmatter with a `status` field and `#status/*` tags, so you can
filter and colour by them in Obsidian's search and graph view:

- `status/living` — kept current, trust it
- `status/built` — describes shipped behaviour
- `status/mixed` — part shipped, part target design; each section says which
- `status/discovery` — Phase 0 thinking, nothing built from it yet
- `status/proposal` — researched, explicitly **not** built or purchased

## Referenced here, but living in the parent monorepo

These filenames appear in the docs and are **intentionally not in this repository**. They are
left as plain text rather than links because a link would necessarily break — the files are
one directory up, in the private XAUUSD monorepo this project was extracted from:

`AGENT_CONSTITUTION.md` · `AGENT_RD_SYSTEM_PROPOSAL.md` · `PROJECT_NOTES.md` · `RESEARCH_LOG.md`
· `quant_platform/KNOWLEDGE_BASE.md` · `agents/`

This is by design, not an oversight. Per [`PROJECT_STATE.md`](PROJECT_STATE.md) "Repo extraction"
and "Methodology extraction", those files hold Meridian-7's live strategy parameters, real
backtest P&L and account numbers. `trading_school/` was split out with `git subtree split`
precisely so that material could never become reachable from this repository's remote.

**They are therefore not backed up by this GitHub repo.** Pushing here does not push them.

## Not in the docs

Code lives in `app/`, schema and seeds in `prisma/`, drafts in `prisma/draft-docs/`. Obsidian
is configured to skip `node_modules/`, `.next/`, `build/`, `coverage/` and other generated
output so search and the graph stay clean.
